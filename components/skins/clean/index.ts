import type { SkinDef } from "../types";
import { Header } from "./Header";
import { Page } from "./Page";
import { Chrome } from "./Chrome";

export const clean: SkinDef = {
  name: "clean",
  label: "Lisse",
  Header,
  Page,
  Chrome,
};
