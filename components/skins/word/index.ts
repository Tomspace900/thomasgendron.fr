import type { SkinDef } from "../types";
import { Header } from "./Header";
import { Page } from "./Page";
import { Chrome } from "./Chrome";

export const word: SkinDef = {
  name: "word",
  label: "Brut",
  Header,
  Page,
  Chrome,
};
