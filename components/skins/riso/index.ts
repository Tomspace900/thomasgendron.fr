import type { SkinDef } from "../types";
import { Header } from "./Header";
import { Page } from "./Page";
import { Chrome } from "./Chrome";

export const riso: SkinDef = {
  name: "riso",
  label: "Encre",
  Header,
  Page,
  Chrome,
};
